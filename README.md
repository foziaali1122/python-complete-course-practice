# Variables, Datatypes & Methods

This notebook covers the fundamental concepts of Python programming including variables, data types, string operations, slicing, and built-in functions. It is part of my Python learning journey focused on building strong programming foundations.

---

## Topics Covered

### 1. Variables & Data Types
- String, Integer, Float, Boolean, NoneType
- Displaying values with their data types using `type()`

### 2. String Slicing
- Indexing and slicing techniques
- Positive and negative indexing
- Reverse strings using slicing

### 3. Immutable Nature of Strings
- Demonstration that strings cannot be modified
- Use of `try-except` to handle errors

### 4. String Methods
- `find()`
- `replace()`
- `count()`
- `split()`
- `join()`
- `strip()`
- `startswith()` / `endswith()`

### 5. Type Conversion
- Converting between `int`, `float`, and `bool`
- Understanding boolean behavior in arithmetic operations

### 6. Input Handling
- Predefined input
- User input using `input()`

### 7. String Formatting
- f-strings
- `.format()` method
- Concatenation

### 8. Built-in Functions Practice
- `isdigit()`
- `isalpha()`
- `isalnum()`
- Indexing and slicing operations

---

## Learning Outcome

- Developed a clear understanding of Python basic syntax
- Learned how to manipulate strings effectively
- Practiced type conversion and input handling
- Built strong foundation for advanced topics like conditions, loops, functions and oops

---

## File Type

This is a Jupyter Notebook (`.ipynb`) file which includes:
- Code examples
- Outputs
- Step-by-step practice

---

## Purpose

The purpose of this notebook is to strengthen core Python concepts required for:
- Data Science
- Machine Learning
- Problem Solving
